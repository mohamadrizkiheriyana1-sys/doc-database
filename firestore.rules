rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Extremely permissive rules for development
      allow read, write: if true;
    }
  }
}
